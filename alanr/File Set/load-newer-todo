Things to do about load newerFix it so that fasl files are not loaded for project defs.Fix it so that if you get a problem loading the fasl, you deletethe fasl and try again.Fix it so that it searches for the set breadth first.Fix it so that it uses the fancy find file stuff when searching.Bias it so that if it finds a directory "foo" and it is looking for "foo-file-set" it tries that directory.Make truename never!! break. What a pain.Use replacetext to modify fasl files to point to new fileset.(#_replacetext 